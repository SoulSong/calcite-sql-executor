{
  "version": "1.0",
  "defaultSchema": "TEST_CSV",
  "schemas": [
    {
      "name": "DYNAMODB_DYNAMIC",
      "type": "custom",
      "factory": "com.shf.calcite.dynamodb.dynamic.DynamoDbSchemaFactory",
      "operand": {
        "accessKey": "access_key_id",
        "secretKey": "secret_key_id",
        "region": "us-east-1",
        "endpoint": "http://127.0.0.1:8765"
      }
    }
  ]
}